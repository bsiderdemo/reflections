What happens when you initialize a repository? Why do you need to do it?
I need to add items to it and then make your first commit to start using version control system.

How is the staging area different from the working directory and the repository? What value do you think it offers?
It is a containner that git uses to pack all related changes and label it as one commit.

How can you use the staging area to make sure you have one commit per logical change?
All files that compose a logical change must be added to staging are to become later into a commit id.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
experiment a solution or a new feature

How do the diagrams help you visualize the branch structure?
The grapsh shows all different paths from init commit and it could use to merge branches or identify what commits are unreacheable from ever label

What is the result of merging two branches together? 
The combination of two branches in a single one

Why do we represent it in the diagram the way we do?
because the graphical representation is more easy to understand

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
pros: there are a lot of time saving
cons: eventually you need to merge manually